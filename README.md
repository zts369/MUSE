# MUSE
Beri, Ding, Lee, Sio

1. Run node server.js               --- to import the MongoDB database connection
2. Ctrl + C                         --- exit the server.js
3. Run node migratePasswords.js     --- hash the existing passwords in the database
4. Run node server.js               --- run the project again
5. Enter localhost:3000 in your browser
